import Vue from 'vue';
import {
    Slider,
    Button,
    Select,
    Option,
    Icon,
    Collapse,
    CollapseItem,
    Image
} from 'element-ui';
Vue.use(Button)
    .use(Slider)
    .use(Select)
    .use(Option)
    .use(Icon)
    .use(Collapse)
    .use(CollapseItem)
    .use(Image);